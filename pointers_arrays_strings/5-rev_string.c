#include "main.h"
#include <stdio.h>
#include <string.h>
/**
 * rev_string - reverse a string
 * @s: a pointer of char type.
 * Return: void.
 */
void rev_string(char *s)
{
int i;
int size = strlen(s);
char change;
char aux[];
while (count < size--)
{
change = s[size];
aux[size++] = s[size];
s[size] = change; 
}
}
   
