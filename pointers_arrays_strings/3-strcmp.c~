#include "main.h"
#include <stdio.h>
#include <string.h>
/**
 * _strcmp - check the code
 * @s1: a pointer of char type.
 * @s2: a pointer of char type.
 * Return: an int.
 */
int _strcmp(char *s1, char *s2)
{
  int i;
  int number;
  int size1 = strlen(s1);
  int size2 = strlen(s2);
  if (size1 >= size2)
    {
      number = size2;
    }
  else if (size1 < size2)
    {
      number = size1;
    }
  for (i = 0; i < number; i++)
    
