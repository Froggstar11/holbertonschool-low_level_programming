#include "main.h"
#include <stdio.h>

/**
 * main - check the code
 * @argc: an int.
 * @argv: an array of char.
 * Return: Always 0.
 */
int main(int __attribute__((unused)) argc, char *argv[])
{
printf("%s\n", argv[0]);
return (0);
}
