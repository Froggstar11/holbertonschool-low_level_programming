#include "main.h"
#include <stdio.h>
/**
 * main - prints name.
 * @argc: an int.
 * @argv: a pointer of int array.
 * Return: 0.
 */
int main(__attribute__ ((unused)) int argc, char *argv[])
{
printf("%s", argv[0]);
return (0);
}
