#include "main.h"
#include <stdio.h>

/**
 * main - check the code
 * @argc: an int.
 * @argv: an array of char.
 * Return: Always 0.
 */
int main(int argc, char __attribute__((unused)) *argv[])
{
printf("%i\n", argc - 1);
return (0);
}
