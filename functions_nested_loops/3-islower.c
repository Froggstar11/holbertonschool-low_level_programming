#include <stdio.h>
#include <stdlib.h>
#include <stdio.h>
#include <time.h>
#include "main.h"
/**
 * _islower - checks if 
 * @c: an int.
 * Return: 1 if c is lowercase, 0 otherwise
 **/
int _islower(int c)
{
if (c > 96 && c < 123)
return (1);
return (0);
}
