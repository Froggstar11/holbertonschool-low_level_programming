#include <stdio.h>
#include <stdlib.h>
#include <stdio.h>
#include <time.h>
#include "main.h"
/**
 * add - add two integers.
 * @first: an int.
 * @second: an int.
 * Return: the result of the adds
 **/
int add(int first, int second)
{
return (first + second);
}
