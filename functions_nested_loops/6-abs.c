#include "main.h"
#include <stdio.h>
#include <stdlib.h>
/**
 * _abs - check the code.
 * @n: an int.
 * Return: absoulte number of n.
 */
int _abs(int n)
{
int absNumber = abs(n);
return (absNumber);
}
