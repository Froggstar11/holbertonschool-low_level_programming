#include <stdio.h>
#include <stdlib.h>
#include <stdio.h>
#include <time.h>
#include "main.h"
/**
 * _abs - computes the absolute value of an integer.
 * @n: an int.
 * Return: n.
 **/
int _abs(int n)
{
if (n < 0)
return (n * (-1));
return (n);
}
